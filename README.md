# ⚽ 足球赛程日历生成器（Football Calendar Generator）

自动生成和同步足球赛程的 ICS 日历文件，支持 iOS、macOS、Android 等各大应用同步。

## 🌟 功能特点

- 📅 **实时赛程生成** - 从懂球帝实时爬取最新赛程数据
- 🔄 **自动同步更新** - 改期、延期自动同步到日历应用
- 🌍 **多联赛支持** - 包含中超、英超、西甲、德甲、法甲、意甲
- 📱 **跨平台兼容** - iOS/macOS、Android、Windows、web calendar 等
- 🚀 **零配置部署** - 一键部署到 Vercel、Render 等平台
- 🔒 **隐私保护** - 无需登录，开源透明

## 📦 项目结构

```
football-calendar/
├── index.html              # 前端界面（Vue.js）
├── main.py                 # FastAPI 后端应用
├── api/
│   └── index.py            # Vercel 部署入口
├── requirements.txt        # Python 依赖
├── vercel.json            # Vercel 部署配置
├── runtime.txt            # Python 版本指定
├── .gitignore             # Git 忽略文件
└── README.md              # 本文件
```

## 🚀 快速开始

### 本地开发

1. **克隆项目**
   ```bash
   git clone <your-repo-url>
   cd football-calendar
   ```

2. **安装依赖**
   ```bash
   pip install -r requirements.txt
   ```

3. **启动服务**
   ```bash
   # 启动 FastAPI 后端
   uvicorn main:app --reload --host 0.0.0.0 --port 8000
   
   # 新终端：启动前端静态服务
   python -m http.server 5500
   ```

4. **访问应用**
   - 打开浏览器: http://localhost:5500

### 部署到 Vercel

#### 方式 1：使用 Vercel CLI（推荐）

1. **安装 Vercel CLI**
   ```bash
   npm install -g vercel
   ```

2. **登录 Vercel**
   ```bash
   vercel login
   ```

3. **部署**
   ```bash
   vercel
   ```

4. **部署完成后**
   - 访问 Vercel 提供的生产 URL
   - 设置自定义域名（可选）

#### 方式 2：使用 GitHub 自动部署

1. **推送到 GitHub**
   ```bash
   git add .
   git commit -m "Initial commit"
   git push origin main
   ```

2. **在 Vercel 连接 GitHub 仓库**
   - 访问 [Vercel Dashboard](https://vercel.com/dashboard)
   - 点击 "Add New" → "Project"
   - 选择你的 GitHub 仓库
   - 点击 "Deploy"

3. **自动部署**
   - 每次推送到 main 分支都会自动部署
   - 预览链接会在 PR 中自动生成

## 📝 API 文档

### 获取球队赛程日历

**请求**
```
GET /api/calendar?team_id=50000330&team_name=北京国安
```

**参数**
- `team_id` (必需) - 球队在懂球帝的 ID
- `team_name` (可选) - 球队名称，用于文件名和日历标题

**响应**
- 内容类型: `text/calendar; charset=utf-8`
- 格式: ICS (iCalendar)
- 状态码: 200 (成功) / 400 (缺少参数) / 500 (爬取失败)

**示例**
```bash
# 获取北京国安赛程
curl "https://your-domain.vercel.app/api/calendar?team_id=50000330&team_name=北京国安" \
  -o "北京国安.ics"
```

### 健康检查

**请求**
```
GET /health
```

**响应**
```json
{
  "status": "healthy",
  "timestamp": "2026-02-15T13:45:36.123456"
}
```

## 🎯 球队 ID 参考

### 中超联赛
| 球队 | ID |
|------|-----|
| 北京国安 | 50000330 |
| 上海海港 | 50007190 |
| 山东泰山 | 50000335 |
| 武汉三镇 | 50077677 |
| ... | ... |

### 英超
| 球队 | ID |
|------|-----|
| 曼城 | 50000529 |
| 阿森纳 | 50000513 |
| 利物浦 | 50000516 |
| 曼联 | 50000515 |
| ... | ... |

完整列表请查看 `index.html` 中的 `db` 对象。

## 🔧 技术栈

**前端**
- **HTML/CSS/JavaScript** - 基础框架
- **Vue.js 3** - 交互框架
- **Tailwind CSS** - 样式框架
- **Font Awesome** - 图标库

**后端**
- **FastAPI** - Web 框架
- **Uvicorn** - ASGI 服务器
- **BeautifulSoup4** - Web 爬虫
- **Requests** - HTTP 库

**部署**
- **Vercel** - 云平台（推荐）
- **Render** - 云平台（备选）
- **自托管** - Docker / 虚拟主机

## 📱 订阅方法

### iOS / macOS
1. 在前端界面选择球队
2. 点击 "iOS/Mac 订阅" 按钮
3. 自动打开日历应用并添加订阅

### Android / Google Calendar
1. 在前端界面选择球队
2. 点击 "复制链接" 按钮
3. 在 Google Calendar 中添加 > 从 URL 订阅
4. 粘贴复制的链接

### Outlook / Windows
1. 复制日历链接
2. 在 Outlook 中：文件 > 打开和导出 > 导入
3. 选择从 URL 导入
4. 粘贴链接

## ⚙️ 环境变量（可选）

```bash
# .env 文件（开发用）
PYTHONUNBUFFERED=1
```

Vercel 部署中已自动配置相关环境变量。

## 🐛 常见问题

### Q: 为什么显示"赛程待更新"？
A: 可能原因：
- 球队 ID 错误
- 数据源暂无此球队的赛程数据
- 网络连接问题

**解决方案**：
1. 检查球队 ID 是否正确
2. 直接访问懂球帝网站查看此球队是否有赛程
3. 稍后重试

### Q: 日历无法自动更新？
A: iOS 日历应用可能需要手动刷新或过几小时自动刷新。
- iOS：下拉日历列表刷新
- Google Calendar：自动每小时刷新
- Outlook：可手动设置同步频率

### Q: 能否添加其他数据源？
A: 欢迎提交 PR！当前支持懂球帝，可扩展支持其他来源（如 ESPN、官方 API 等）。

### Q: 部署到 Vercel 后 API 报 500 错误？
A: 检查以下步骤：
1. 确保 `api/index.py` 存在且正确导入 `main.py`
2. 检查 `requirements.txt` 中所有依赖都有指定版本号
3. 查看 Vercel 的函数日志（Vercel Dashboard > Deployments > Logs）
4. 删除 `__pycache__` 并重新部署: `vercel --prod`

## 🔐 安全与隐私

- ✅ 完全开源，代码透明
- ✅ 无需登录，隐私保护
- ✅ 仅负责爬取公开数据
- ✅ 无数据推送，纯前端交互

## 📜 许可证

MIT License - 自由使用、修改和分发

## 🤝 贡献

欢迎提交 Issue 和 PR！

### 如何贡献：
1. Fork 本仓库
2. 创建特性分支: `git checkout -b feature/new-feature`
3. 提交更改: `git commit -am 'Add new feature'`
4. 推送分支: `git push origin feature/new-feature`
5. 提交 Pull Request

## 📞 联系方式

- 📧 Email: chenbeiqi@example.com
- 🐦 Twitter: [@chenbeiqi](https://twitter.com/chenbeiqi)
- 💬 GitHub Issues: [football-calendar/issues](https://github.com/chenbeiqi/football-calendar/issues)

---

**⭐ 如果觉得好用，请给个 Star！**

祝你使用愉快！🎉
